# Promises and the event loop

These examples explore promise reactions, timers, and the ordering of synchronous work, microtasks, and tasks.

Run the event-order example from the repository root:

```bash
npm run example -- studies/async/promises/event-loop-order.ts
```

Before running it, predict the complete output sequence and then explain which lines come from synchronous execution, promise microtasks, and timer tasks.
