# Playground

Use this directory for disposable experiments that do not yet belong elsewhere. Everything here except this README is ignored by Git.

Move useful work into `studies`, `practice`, `problems`, `algorithms`, `data-structures`, or `projects` once its purpose becomes clear.
