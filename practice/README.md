# Practice

Practice contains topic-driven TypeScript drills that do not come from a formal coding challenge. Group related exercises by topic, such as `types/`, `generics/`, `async/`, or `object-model/`.

A practice file should state its goal in a short comment and remain small enough to run directly with `npm run example -- <path>`.
